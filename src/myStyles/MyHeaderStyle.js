import { StyleSheet } from "react-native";

export default myHeaderStyle = StyleSheet.create({
  container: {
    paddingTop: 0,
    shadowOffset: {
      width: 0,
      height: 2
    },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
    elevation: 5
  }
});
